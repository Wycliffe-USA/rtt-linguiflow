language: bash
dist: trusty
sudo: required

before_install:
    - sudo apt-get update -q
    - sudo apt-get install -y git make build-essential clang libboost-all-dev cmake unzip python-pip python-scipy

script: 
    - sudo bash ./test.sh

notifications:
  slack: ftm:SB5Aq83xLhVCjlyvXGYBaxru